{
  "name": "@saas/api",
  "version": "0.1.0",
  "private": true,
  "main": "src/app.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn src/app.ts",
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.8.2",
    "@fastify/sensible": "^6.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "*",
    "pino-pretty": "^13.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "*",
    "vitest": "^4.1.0"
  }
}
